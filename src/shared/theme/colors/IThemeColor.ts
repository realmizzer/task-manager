type Color = string;

export interface IThemeColor {
  primary: Color;
  text: Color;
  description: Color;
  hint: Color;
  card: Color;
  input: Color;
  background: Color;
  important: Color;
  success: Color;
  fail: Color;
  white: Color;
  black: Color;
}
