type Color = string;

export interface IThemeColor {
  text: Color;
  description: Color;
  hint: Color;
  background: Color;
  important: Color;
  success: Color;
  fail: Color;
  white: Color;
  black: Color;
}
